const { createClient } = require('@supabase/supabase-js');

const RIN_SUPABASE_URL = 'https://zyoszbmahxnfcokuzkuv.supabase.co';
const RIN_SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inp5b3N6Ym1haHhuZmNva3V6a3V2Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzM1MDU3OTMsImV4cCI6MjA4OTA4MTc5M30.Ilz4RYTcgZU3IMnABg0eV7iAfFcC0iykyl4DOln-mjY';

const rinSupabase = createClient(RIN_SUPABASE_URL, RIN_SUPABASE_ANON_KEY);

async function setupRinGtm() {
  try {
    console.log('🔧 Setting up RIN GTM in Supabase...\n');

    // Test connection
    const { data: testData, error: testError } = await rinSupabase
      .from('gtm_drafts')
      .select('*')
      .limit(1);

    if (testError && testError.code === 'PGRST116') {
      console.log('📋 gtm_drafts table does not exist in RIN Supabase');
      console.log('⚠️  Need to create table manually in Supabase SQL editor');
      console.log('\n📝 SQL to run in Supabase:\n');
      console.log(`
CREATE TABLE IF NOT EXISTS public.gtm_drafts (
  id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  draft_date DATE NOT NULL UNIQUE,
  draft_type VARCHAR(255),
  channel VARCHAR(255),
  title TEXT,
  content TEXT,
  status VARCHAR(50) DEFAULT 'draft',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_gtm_drafts_date ON public.gtm_drafts(draft_date);

ALTER TABLE public.gtm_drafts ENABLE ROW LEVEL SECURITY;
GRANT SELECT, INSERT, UPDATE, DELETE ON public.gtm_drafts TO anon, authenticated;
      `);
      return;
    }

    if (testError) {
      console.log('❌ Connection error:', testError.message);
      return;
    }

    console.log('✅ gtm_drafts table exists in RIN Supabase');
    console.log('📊 Current records:', testData?.length || 0);

    // Migrate RIN carousels
    console.log('\n📦 Migrating RIN carousels...\n');

    const carousels = [
      {
        draft_date: '2026-05-04',
        title: 'RIN: Dispatch Intelligence',
        content: `# RIN: Dispatch Intelligence

## Slide 1: The Problem
Manual dispatch = 2+ hours/day wasted.
Double bookings. Idle drivers. Chaos.

## Slide 2: Smart Matching
Real-time optimization.
Drivers know their next job before they finish the current one.

## Slide 3: The Result
2 hours saved per day.
More jobs assigned.
Drivers stay longer.

## Slide 4: The Call
Fair dispatch. Real-time optimization. Built for operators.

**Join the network:** drive.roadside.ai`,
        channel: 'instagram',
        draft_type: 'carousel',
        status: 'approved'
      },
      {
        draft_date: '2026-05-11',
        title: 'RIN: Smart Matching Saves 2 Hours/Day',
        content: `# RIN: Smart Matching Saves 2 Hours/Day

## Slide 1: The Problem
Manual dispatch = chaos.
2+ hours wasted. Double bookings. Idle drivers.

## Slide 2: The Solution
Real-time matching. Drivers know their next job before finishing the current one.

## Slide 3: The Impact
2 hours saved per day.
More jobs assigned.
Drivers earn more.

## Slide 4: Join RIN
Fair dispatch. Real-time optimization. For operators, by operators.

**drive.roadside.ai**`,
        channel: 'instagram,facebook',
        draft_type: 'carousel',
        status: 'approved'
      }
    ];

    for (const carousel of carousels) {
      const { data, error } = await rinSupabase
        .from('gtm_drafts')
        .upsert([carousel], { onConflict: 'draft_date' });

      if (error) {
        console.log(`❌ ${carousel.draft_date}: ${error.message}`);
      } else {
        console.log(`✅ ${carousel.draft_date}: "${carousel.title}"`);
      }
    }

    console.log('\n✅ RIN GTM setup complete!');
  } catch (err) {
    console.error('❌ Error:', err.message);
  }
}

setupRinGtm();
